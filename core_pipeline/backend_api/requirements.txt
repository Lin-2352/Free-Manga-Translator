# Pinned 2026-07-09 (audit round A3): 0.108.0 was ~2 years stale and missed Starlette security
# patches since. Verified against this project's routes (all 29 register correctly, header-gate
# dependency and CORS preflight both behave as expected) before bumping -- see DEVELOPMENT_NOTES.md.
fastapi==0.128.0
uvicorn[standard]>=0.51.0
pydantic>=2.7.0
python-dotenv>=1.0.1
# paddlepaddle is deliberately NOT listed here -- see python/requirements.txt's comment for why
# a bare `paddlepaddle` line silently resolves to a CPU-only wheel that shadows paddlepaddle-gpu.
# Install paddlepaddle-gpu explicitly, from the CUDA-specific index, after this file.
paddleocr>=3.6.0
