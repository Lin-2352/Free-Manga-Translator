# Backend API layer
fastapi==0.128.0
uvicorn[standard]>=0.51.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# Pipeline ML/imaging layer. torch/torchvision are exact-pinned because model-loading code
# assumes a specific major API shape; install the CUDA build from the PyTorch index, not plain
# PyPI, e.g.:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
onnxruntime-gpu>=1.26.0
opencv-python>=4.10.0
numpy>=2.3.0,<3.0.0
Pillow>=12.0.0
transformers>=5.12.0
easyocr>=1.7.2
manga-ocr>=0.1.14
requests>=2.34.0

# Model-specific runtimes backing step 1/5's detection/OCR models
paddlepaddle>=3.3.1
paddleocr>=3.6.0
# ultralytics (YOLOv11n-seg) backs the bubble-segmentation model. Note: this package is
# AGPL-3.0 licensed; review your distribution/usage against that license.
ultralytics>=8.4.0

# Optional: improves API token-usage estimate accuracy in api_manager.py; already wrapped in a
# try/except with a heuristic fallback, so its absence does not break anything.
tiktoken>=0.8.0
